enum Role {
  GAMEMASTER
  USER
}

type AuthInfo {
  token: String!
  player: Player!
}

extend type Query {
  me: Player
}

extend type Mutation {
  login(name: String!, password: String!): AuthInfo
  signUp(name: String!, password: String!): AuthInfo
}
