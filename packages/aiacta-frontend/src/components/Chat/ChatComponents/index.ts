import { Text } from '@mantine/core';
import { DieIcon } from './DieIcon';
import { RollResult } from './RollResult';

export const components = {
  Die: DieIcon,
  RollResult: RollResult,
  p: Text,
};

export { RollingContext } from './RollingContext';
